//Declarando variables
var filtrarRestaurant = $(#filtrar);
//tomar el valor que el usuario agrega a los inputGroupSele
var filtrado = $filtrar.val();

//haciendo la funcion para que aparezcan los tipos de restaurantes
function showRestaurants() {
  $filtrar.keyup
}

//Funcion que valida entrada de input

function
